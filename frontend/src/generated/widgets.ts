/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Widget Catalogue
 * A deliberately tiny contract. It exists to show two different ways a
generated client and a generated server can drift apart while both sides
look correct.

 * OpenAPI spec version: 1.0.0
 */
export interface WidgetList {
  /** Always present. */
  generatedAt: string;
  /** Optional, and NOT nullable. Either the key is absent, or its value is an array. `null` is not a legal value here. */
  items?: Widget[];
}

export interface Widget {
  id: string;
  /** Optional. A server is free to leave this out; a client must cope with it being absent. */
  label?: string;
}

export type ListWidgetsParams = {
/**
 * Which server implementation to exercise.
 */
scenario: ListWidgetsScenario;
};

export type ListWidgetsScenario = typeof ListWidgetsScenario[keyof typeof ListWidgetsScenario];


// eslint-disable-next-line @typescript-eslint/no-redeclare
export const ListWidgetsScenario = {
  ok: 'ok',
  'nil-slice': 'nil-slice',
  'omit-optional': 'omit-optional',
} as const;

/**
 * @summary List widgets
 */
export const getListWidgetsUrl = (params: ListWidgetsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `http://localhost:8099/widgets?${stringifiedParams}` : `http://localhost:8099/widgets`
}

export const listWidgets = async (params: ListWidgetsParams, options?: RequestInit): Promise<WidgetList> => {
  
  const res = await fetch(getListWidgetsUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: WidgetList = body ? JSON.parse(body) : {}
  return data
}
